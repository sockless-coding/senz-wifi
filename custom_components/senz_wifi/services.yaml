boost:
  target:
    entity:
      integration: senz_wifi
      domain: climate
  fields:
    duration_minutes:
      friendly_name: Duration (minutes)
      description: Duration of the boost in minutes (default: 180)
      selector:
        number:
          min: 1
          max: 1440
          unit_of_measurement: minutes
          mode: box
